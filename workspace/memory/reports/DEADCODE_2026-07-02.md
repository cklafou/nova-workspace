# Dead-code removal — prefrontal_cortex.py (Thoughts-cycle orphan)
_Last updated: 2026-07-08 08:37:33_
_2026-07-02, Fable. Executed the browser-Opus review's removal prompt, verified against the live
codebase first per its own step 2._

## Verified before removal

- **No live import of `prefrontal_cortex` or `NovaBrain` anywhere.** Only hits: audit metadata
  (`audit_queue.py`, `audit_scripts.py` name lists), `restructure.py` docstring examples, and the
  module itself. Its declared consumers (`circadian.py`, `agent_loop.py`, `Thoughts/`,
  `THOUGHT_TEMPLATE.md`) don't exist in the repo — the review was right.
- **`rules.py` is NOT dead — review's scope adjusted.** `nova_motor/motor_cortex.py:50` imports
  `OPERATIONAL_DIRECTIVES, TASKBAR_X, TASKBAR_Y` from it. Stays.
- **`checkin.py` stays too:** no Python importer, but `rules.py`'s `OPERATIONAL_DIRECTIVES`
  (fed to Nova via the motor layer) instruct her to *run* `nova_cortex.checkin` between actions.
  Deleting it would plant a broken instruction — worse confusion than the corpse. If the motor
  layer's directives get rewritten later, revisit both.
- **`Master_Inbox` is NOT dead-architecture-only:** live `server.py` (`_maybe_route_inbox`, runs on
  every AI reply) and `injector.py` route module responses there. Same name, live feature — out of
  scope, untouched.

## What changed

- `nova_body/nova_cortex/prefrontal_cortex.py` → **archived** to
  `_admin/archive/prefrontal_cortex_retired_2026-07-02.py` (house convention is archive-don't-
  delete, per the 05-27 note in `nova_cortex/__init__.py`; git history preserves it regardless).
  Stale `__pycache__` entry removed.
- Stale references cleaned: `rules.py` `REQUIRED_MODULES` (entry removed so
  `verify_core_modules()` won't warn), `nova_cortex/calls.md` (section removed),
  `memory/STATUS.md` (module row), `nova_cortex/__init__.py` (docstring now records what was
  archived, why rules/checkin stay), `SELF/reference/upgrade_protocol.md` (package tree).
- `executive.py`, `runtime.py`, and all live logic: **untouched.**

## Verification

Sandbox import pluck passes clean post-removal: `nova_cortex` + `executive`, `tasking`,
`context_builder`, `nova_status`, and `nova_runtime.runtime` (+ `event_bus`, `transcript_store`)
all import with no reference errors. Full live boot check = Cole's next restart (llama +
Windows-only deps can't run in the sandbox): boot log should show the usual healthy sequence and
`python -m nova_runtime` should still come up headless. `FILE_INDEX.md` and the body manifest
regenerate themselves via the watcher/manifest pass — not hand-edited.

One brain: the executive. The competing cognitive design is out of her body.
