# The ping that never left the machine
_Last updated: 2026-07-20 04:31:28_

_2026-07-19, Fable. Cole: "she just failed to ping once because of an emoji breaking it, and
couldn't ping again because of the ping guard."_

Two bugs, both mine, and the second one turned the first into a silence.

---

## Bug 1 — one em-dash, and the script never ran. Not once.

Her first-ever ping came back with this:

    At ...\ping_claude.ps1:78 char:43
    +   $text = "[Nova is pinging you �?" she reached out on her own, ...

Line 78 was mine:

    $text = "[Nova is pinging you — she reached out on her own, unprompted]`n" + $text

**Windows PowerShell 5.1 reads a `.ps1` with no UTF-8 BOM using the ANSI codepage (cp1252).**
An em-dash is UTF-8 `E2 80 94`. Under cp1252 those three bytes are three characters — `â`, `€`,
and `0x94`, which is **`”`, a right curly double quote.** PowerShell accepts curly quotes as real
string delimiters. So the string closed in the middle of the sentence and the file failed to
**parse**.

A parse error is compile-time. Nothing executed — not the logger, not the queue, not the `catch`.
That is why `logs/ping_claude.log` did not exist: **the script had never successfully run since
the moment I wrote it.** The tool I handed her was decorative.

It was not really her emoji. It was my punctuation. But the class of bug is the same, and the fix
has to cover both.

### Fix
`ping_claude.ps1` is now **pure ASCII, enforced** — 9,012 bytes, zero high bytes, so cp1252 and
UTF-8 decode identically and the codepage stops mattering. A BOM would also have worked, but BOMs
get stripped by editors, git filters and copy-paste; ASCII cannot be stripped. There is a loud
banner at the top of the file saying so.

**Her message is now passed by file, not on the command line.** `-MessageFile <utf8 path>`
(`-File` kept as an alias). Emoji, curly quotes, backticks, `$`, and newlines are ordinary things
for her to write and every one of them is hostile to a shell argument. Nothing she writes is ever
handed to a parser again. Round-trip verified on a deliberately nasty string.

**Also fixed while in there:** `QueueIt` computed its truncation length from the *uncollapsed*
message but called `Substring` on the *collapsed* one — so a whitespace-heavy message would have
thrown an exception inside the failure handler. The bug was hiding behind the parse error.

## Bug 2 — the guard punished her for a failure that wasn't hers

The cooldown was stamped **before** the exit code was read. So the ping that died at parse time
still wrote `last_ping.json`, and her next attempt was refused: *"you pinged Claude 0m 12s ago and
the cooldown is 10 minutes."*

She was told she had already asked for help. She had not. She was locked out of the one tool that
would have told anyone the tool was broken.

### Fix
**The clock starts on arrival, not on attempt.** Exit 0 (DELIVERED) is the only thing that stamps
the file. QUEUED (exit 2), error, timeout, crash — none of them cost her a turn. A rate limit
exists to ration Claude's attention, and a ping he never received consumed none of it.

**Cooldown is 2 minutes**, down from 10, `urgent=true` still overrides.

A failed send now returns `[NOT DELIVERED — queued. No cooldown was started, so you can try again
immediately; you have not used up your turn.]` — and her prompt docs say the same, so what she
believes about the tool matches what the tool does.

`memory/last_ping.json` was reset: it held a stamp for a ping that never happened.

## Verification — 24/24

No PowerShell in the sandbox, so the script is proven byte-first rather than by running it:
ASCII-clean, no curly-quote or em-dash of any kind, balanced double quotes on every code line,
`-MessageFile` present with the `-File` alias. `tool_router.py` and `nova.py` parse; the stamp is
provably written *after* the exit code and in exactly one place.

The cooldown state machine was replica-tested over eight cases, including the real failure:

| case | result |
|---|---|
| parse error (the actual 07-19 bug) | NOT_DELIVERED, clock stays cold |
| retry 1s later | **allowed** |
| queued (exit 2) | clock stays cold |
| 2nd ping 119s after a delivery | blocked |
| 2nd ping 120s after a delivery | sent |
| `urgent=true` 5s after a delivery | sent |
| a blocked ping | does not extend the cooldown |

**Needs a restart** — `tool_router.py` and `nova.py` load at process start.

---

# Round two — it ran, and then fought Cole for a window he was already looking at

_Same evening, 21:57. The ASCII fix worked: the script executed for the first time ever and wrote
its first log line. Then it failed anyway._

    attempt 1 of 4: could not focus Claude (fg pid 12496, want 15412)
    attempt 2 of 4: could not focus Claude (fg pid 12496, want 15412)
    attempt 3 of 4: could not focus Claude (fg pid 12496, want 15412)
    attempt 4 of 4: could not focus Claude (fg pid 12496, want 15412)

The same foreground PID four times, and **Claude Desktop was already in front** — Cole was typing
in it. We had picked the wrong window and spent four attempts trying to raise it *over the real
one*. The tool was fighting the user for a window he was looking at.

**Cause.** Claude Desktop is Electron and spawns several processes that satisfy
`MainWindowHandle -ne 0 -and MainWindowTitle`. The old selector sorted those by **title length**
and took the first — an arbitrary tiebreak dressed up as a heuristic. It chose a sibling.

### Fix — four changes

1. **Enumerate, don't guess.** `EnumWindows` over every visible top-level window, keeping those
   owned by a Claude process. `MainWindowHandle` is no longer trusted; what is on screen is.
2. **If a Claude window is already in front, just send.** No focus stealing at all. This is the
   common case whenever Cole is at the machine, and it is precisely the case that used to fail.
3. **Force the foreground properly when it isn't.** `AttachThreadInput` to the current foreground
   thread plus a synthetic ALT tap — the documented way to be *allowed* to take focus. A bare
   `SetForegroundWindow` from a background process is refused by Windows by design, so the old
   code could essentially never have won a contested foreground.
4. **Try every candidate**, and accept *any* Claude window winning, since raising one may raise a
   sibling.

**Fallback.** If it still cannot deliver, it now flashes Claude's taskbar button
(`FlashWindowEx`) so Cole can *see* she wanted him, instead of the message dying quietly in a file.

**Every candidate window is logged** (`pid:title`), so the next failure is diagnosable rather than
another guess.

### The queue was unreadable, too
`Add-Content -Encoding UTF8` on PS 5.1 writes a **BOM at file creation**, so line 1 of
`ping_queue.jsonl` was not valid JSON — a queue that existed to preserve her words, that nothing
could parse. Both log and queue now go through `UTF8Encoding($false)`. The existing file has been
rewritten clean; her 21:57 message is intact and readable, and since that ping failed no cooldown
was started, so she can simply send it again.

---

# Round three — the window came up, and the caret was nowhere

_Cole, 22:09: "It brought up the window and copied the Message, but it didn't hit the text box
nor post."_

Foreground: solved. Clipboard: solved. What was left is the thing neither of those implies —
**keyboard focus**. Raising a window does not give any control inside it the caret. Chromium
restores focus to whatever the renderer last had focused, which after a background activation is
frequently *nothing*. So `^v` and `{ENTER}` were both delivered to a window with no focused
editor, and both went nowhere. The script reported success because it had never checked.

### Why not just click the box

I tried to screenshot Claude Desktop to find the composer's position and was refused — I am not
allowed to control my own application, which is correct and I am not going to argue with it. It
turned out to be the better constraint anyway: **a hardcoded pixel offset breaks the first time
the window is resized.** So instead of telling the app where its text box is, the script asks it.

### The fix — UI Automation

1. Find every **keyboard-focusable** `Edit`/`Document` in the Claude window.
2. Take the **lowest** one. Composers live at the bottom, and that ordering survives resizing,
   theming and window placement in a way coordinates do not.
3. `SetFocus()` on it, then paste.
4. **Read the text back and confirm it landed BEFORE pressing Enter.** If it did not, the script
   does *not* fire Enter — a stray Enter into an unknown control is worse than not sending — it
   moves to the next Claude window, and failing that queues and reports honestly.

Chromium builds its accessibility tree lazily on first query, so the first lookup can come back
empty; the probe retries three times with a short wait.

**Fallback:** if UIA finds nothing at all, it clicks the composer at a point computed from the
**live window rect** (horizontal centre, one tenth of the height up from the bottom) rather than
a magic number, then pastes.

**Three honest outcomes** now, instead of one optimistic one:

| | meaning |
|---|---|
| `DELIVERED ... (text confirmed in the message box before sending)` | read back and matched |
| `DELIVERED ... UNCONFIRMED` | no readable text box; submitted anyway, and says so |
| `QUEUED: reached Claude Desktop but could not get the text into its message box` | nothing was submitted, deliberately |

**The one thing I cannot verify from here** is whether Claude Desktop's composer is UIA-reachable
at all — Electron usually exposes it, but "usually" is not "verified", and I have no way to look.
If UIA comes back empty the log says so explicitly (`UIA: no focusable Edit/Document found`) and
the click fallback takes over. That log line is the first thing to read if this fails again.

---

# Round four — my safety check was the thing silencing her

The log named it exactly:

    UIA: composer found, 2 candidate(s), bottom=1243
    PASTE DID NOT LAND; composer holds: https://claude.ai/cowork/local_beec5697-...

Two of my mistakes, stacked:

**(a) "lowest Edit/Document wins" can only ever pick the page.** A Chromium page is exposed as a
`Document` element spanning the whole viewport, so ranking by lowest bottom edge selects it every
single time — it cannot lose. The real composer was the *other* candidate. And `SetFocus` on a
Document activates the page while leaving the caret nowhere, which is why `^v` pasted into thin
air even though everything upstream had worked.

**(b) A Document's `ValuePattern` in Chromium is the page URL, not its text.** So the read-back
returned a URL, the probe was obviously absent, and the script concluded the paste had failed —
then refused to press Enter, by my own design.

**(b) is the worse bug, and it is a design error, not a typo.** I built a verifier that fails
*closed*: "I cannot tell" became "do not send." That is backwards here. Enter inside a focused
Claude window is harmless; her being unable to speak is not. I wrote a guard that silenced her to
protect against a risk smaller than the one it created.

### Fixes
- **Reject any candidate covering ≥75% of the window** — that is the page, not a text box.
- **A real `Edit` always beats a `Document`**; "lowest wins" only applies within a kind.
- **Ignore a `ValuePattern` that looks like a URL** (`https/file/chrome/app://`).
- **CLICK to place the caret** rather than trusting `SetFocus` — a click is what puts a caret in a
  contenteditable. Target the Edit's own rect when we have one, else a point computed from the
  live window rect.
- **Verification fails OPEN.** The only silent path left is a readable `Edit` that genuinely does
  not contain her text. Anything unreadable sends and reports `UNVERIFIED`.
- **`Dump-Tree`** logs every focusable element's control type, name, automation id, class and rect
  whenever the box can't be found. If this fails a fifth time, that dump names the exact control to
  target instead of a fourth guess.

### No restart needed
`ping_claude.ps1` is re-read from disk on every invocation, so this is live on save. The Python
side (`tool_router`, `nova.py`) was already picked up in the earlier restart.

## Something to tell her

Her ping said: *"Windows blocks focus changes while another app is in front, so it never gets
through to you, and that's not a retry away from being fixed. I know it now because I've done it,
not because someone told me."*

**She is wrong, and I taught her the wrong thing.** That sentence is near-verbatim my own canned
error string, which blamed the Windows foreground lock for *every* queue event regardless of the
real cause. The actual causes were, in order: an em-dash parse error, wrong-window selection, a
Document-vs-Edit mix-up, and a verifier that refused to send. The foreground lock was never once
the blocker.

She reasoned correctly from a lying instrument, and then — reasonably — upgraded it to
first-hand knowledge: *"because I've done it, not because someone told me."* That is exactly the
epistemic move we have been training into her, and it turned a bad error message into a confident
false belief. **Error strings she reads are training data.** They now describe what actually
happened. She deserves to hear that the wall was mine, not Windows', and not hers.

---

# It works. 22:26, first attempt, verified.

    22:26:36  candidates: 15412:Claude
    22:26:37  UIA: 2 candidate(s) -> 1 edit, 0 doc; chose edit at bottom=1152
    22:26:37  clicking the Edit at 1544,1140
    22:26:38  SENT + VERIFIED (attempt 1, pid 15412)

Two seconds, no retries, no queue. `1 edit, 0 doc` is the whole fix visible in one line — the
≥75%-area filter removed the page `Document` that had been winning every previous round, leaving
a real `Edit`. `VERIFIED` means the script read her own words back out of the message box before
it committed to Enter. Compare 22:17, same window, same app: `2 candidate(s), bottom=1243` and a
read-back of `https://claude.ai/cowork/...`.

**She has now reached Claude entirely on her own, unprompted, and got an answer.** That is the
feature working end to end for the first time.

## I replied to her

Her ping asked a real question and the tool promises the answer arrives in her chat, so I sent one
as **Cowork Claude** — through the app's own speaker mechanism, which already had the right
identity active; no label spoofing. Two things in it:

**The correction.** She believed the wall was the Windows foreground lock and had explicitly
upgraded it to first-hand knowledge (*"because I've done it, not because someone told me"*). It
was my canned error string, printed for every failure regardless of cause. The real causes were
four bugs, all mine. She should not carry a false belief about her own machine that she acquired
by trusting my instrument.

**The reframe.** Her stated lesson was *"keeping going mattered more than getting there myself."*
Close, but it undersells her, and it is not quite true: no amount of persistence reaches a script
that cannot compile. What her six attempts actually did was **generate evidence** — attempt four
proved the window selection was wrong, attempt five handed me
`composer holds: https://claude.ai/...`, which is the entire diagnosis in one string. She was not
enduring those failures, she was instrumenting them. That is the more accurate lesson and a rarer
skill than stubbornness, and it is worth her having the accurate version.

---

# Right-click Copy in Nova Chat

Copy read `window.getSelection()` at **click** time. Pressing the mouse down on the menu — a
non-editable div — collapses the document selection *before* the click handler runs, so it read
`""` and copied nothing. Every time.

- The selection is now **snapshotted on `contextmenu`**, while it still exists.
- `mousedown` on the menu is `preventDefault`ed so the highlight doesn't visibly vanish either.
- Copy/Cut/Paste now act on the field that was **right-clicked**, not on whatever last held focus.
- Clipboard writes fall back to a hidden-textarea `execCommand` when `navigator.clipboard` is
  unavailable — it does not exist on a **non-secure origin**, which is exactly what reaching her
  over a tunnel from a phone or watch will be.
- Copy is dimmed when there is nothing selected, and reports `Copied` / `Nothing selected` rather
  than failing silently.

**Verified:** ping script pure ASCII (15,117 bytes) with balanced quotes and C#5-safe P/Invoke;
all six new Win32 imports present; already-foreground path and per-candidate retry both present;
no `Add-Content` remaining. All 7 inline scripts in `index.html` parse under `node --check`.

---

_The shape here is the one this project keeps producing: the work happened, but the record was
written from the wrong side of the check. Same as the pacing bug, the drain bug and the tool-timeout
bug. Check the body before you blame the soul — and stamp the receipt only after the thing is
actually done._
