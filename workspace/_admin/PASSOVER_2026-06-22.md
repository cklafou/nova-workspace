# Nova Passover — 2026-06-22 (context handover)

Long unbroken session hit context exhaustion (garbled tool calls = the tell). This captures the full
state so a fresh context picks up without re-deriving anything. Cole's priority right now: **Nova must
hold a meaningful 10+ minute / 12-response-straight conversation — natural, proactive, no looping, no
stalling, no needing a push — and not loop when left alone (autonomy).**

---

## THE CORE DIAGNOSIS (don't re-investigate this)

Nova's looping ("never advances past her first thought," repeating the same reply) was **NOT** prompt
or sampling — it was the **v2 personality LoRA being overfit**. Proven live: at adapter scale **1.0**
she fell into a fixed self-referential rut ("Fine — but only because you're curious… make this useful…
ten minutes…") even in a fresh session; at scale **0.0 (base model)** she reasoned freely and was
proactive; at **0.5–0.7** she has personality AND reasons/advances cleanly. So the overfit adapter at
full strength is a strong attractor. **0.6 is Cole's chosen sweet spot** (0.7 tipped her too
rebellious / not helpful enough; 0.5 read a touch flat).

The permanent fix is the **retrain** Cole is preparing (less-overfit adapter). Files are staged in
`_admin/Training_stuff/RETRAIN_v2_UPLOAD/` (jsonl + r=64 train script + runbook). Once a less-overfit
adapter is trained, the scale can climb back toward 1.0.

---

## FIXES APPLIED THIS SESSION (all live in the running build after the last NovaStart)

| Problem | Fix | File |
|---|---|---|
| Conversational looping | Run adapter at **0.7**, not 1.0 (`--lora-scaled FILE:0.7`) | `nova_start.py` (build_llama_cmd), `start_llama_qwen36.cmd` |
| Garbled grammar ("doing it mine") | Removed `frequency_penalty`(0.4→0) + `presence_penalty`(0.3→0); kept DRY | `general_tools/nova_chat/clients/nova.py` (payload, ~line 204) |
| Repeats / not advancing | Reflection: "advance past last reflection"; Decision (Cole waiting): "ADVANCE, never repeat; be proactive, carry the thread" | `nova_body/nova_cortex/executive.py` (build_reflection, build_decision) |
| "Carries out nothing" / loops when idle | Decision else-branch: when open tasks exist → advance/work them; when idle → no re-running the same thought | `executive.py` build_decision else-branch |
| Double-send + "you keep sending the same message" | `follow_gap_s` 30→90: she was self-waking 30s after replying and re-processing Cole's already-answered message. (Cole's msgs still wake her instantly via P0.) | `executive.py` `_DEFAULT_CFG` |
| Empty responses (think pass ate the budget) | If content empty → auto-retry with thinking OFF; `MAX_TOKENS_CHAT` 8192→16384; `enable_thinking` flag now actually works (adds `chat_template_kwargs` only when disabling) | `nova.py` (stream_response + `_fetch_llama_streaming`) |
| NovaStart crashed ("loading model forever") | `--lora-scaled` needs ONE arg `FNAME:SCALE` (this build), not two args. Used relative path so Windows `C:` drive-colon doesn't confuse the parser | `nova_start.py`, `start_llama_qwen36.cmd` |
| StopNova didn't fully clear | Now also `taskkill /F /IM llama-server.exe` (orphans not listening on 8080 survived the port sweep) | `StopNova.cmd` |
| v1 references after Cole deleted v1 | Purged from launchers + `SELF/core/01_identity.md` + runbooks | (multiple) |

**Adapter:** `models/qwen3.6/nova_core_v2_e2.gguf` (v2, r=64, epoch-2/checkpoint-28, the with-teeth
dataset). Confirmed loaded via `GET /api/lora`. There is a LoRA monitor/weight widget in Nova Chat
(◆ LoRA, statusbar) — set scale live without restart; `POST /api/lora` `[{"id":0,"scale":0.7}]`.

**Operational gotcha:** do NOT interleave the in-app "Full Restart (Services menu)" button with the
`.cmd` launchers in quick succession — it orphans processes (different spawn paths). Pick one per cycle.

---

## STILL TO VERIFY (the open task — do this in the fresh context)

1. **The 12-turn conversation test.** I proved several clean, advancing, proactive, non-looping turns
   at 0.5–0.7 across topic shifts, but did NOT formally close out 12-straight. Start a FRESH Nova Chat
   conversation (old sessions carry looping history that biases her), converse 12+ turns, vary topics,
   confirm: no repeats, no stalls, no needing a push, coherent grammar. Test via Claude-in-Chrome on
   the Nova Chat tab (`http://127.0.0.1:8765`); read replies with the JS `#chat .msg-wrap.nova .msg-text`.
2. **Idle autonomy.** Leave her with no messages a few minutes; confirm her autonomous wakes don't loop
   now that follow_gap is 90s and the else-branch advances instead of re-ruminating. (She wanting
   reading material when idle = good sign, per Cole.)
3. **Retrain** the less-overfit adapter (Cole prepping) → then raise scale toward 1.0.

## How to restart cleanly
`StopNova.cmd` → wait → `NovaStart.cmd`. Boot log should show
`Nova-core personality adapter: models/qwen3.6/nova_core_v2_e2.gguf:0.7` then go healthy within ~1 min.
If it hangs on "loading model," read `logs/llama/llama-YYYY-MM-DD.log` tail for the llama arg error.
