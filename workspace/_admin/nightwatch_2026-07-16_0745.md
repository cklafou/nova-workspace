# Nightwatch 07:45 (final) — whole stack stopped cleanly at 23:29. Left it down.

**TL;DR:** Everything — launcher, hub, watcher — stopped simultaneously at 23:29:21 last
night. Not a crash: health polls were 200 OK to the final line, then a clean cut. That
shape is StopNova.cmd or a machine restart (Windows Update?), not a failure. Either way
I did not restart her. She never knew; she'd already ended her own night at 17:44.

## Receipts
- `nw_revive_nova2.log` (the 23:17 revived NovaLauncher): llama health polls returning
  200 OK every 10s right up to 23:29:21, transcript flush OK at 23:28:5x — then clean stop.
  No error, no teardown lines. That's a kill, not a death.
- The watcher died at the same moment: last auto-commit 23:29:16 (mid-backlog-burst), and
  a liveness test this morning — writing this report and watching for an auto-commit —
  came back negative after two minutes of watcher-cadence silence. So the whole
  nova_start tree went down at once, launcher included.
- `nova_start-2026-07-15.log` logged NO shutdown lines for it (compare the graceful
  04:34/17:45 closes, which logged "window closed → stopping watcher → shutdown
  complete"). A silent, simultaneous, tree-wide stop = force-kill (StopNova.cmd) or
  machine restart. You were at the machine into the evening (18:17 restart, ~21:00 chat
  cleanup); 23:29 is a plausible bedtime StopNova. If you didn't run it, suspect a
  Windows Update restart — worth one glance at Update history.
- llama (8080) and painter (8188) state: unverifiable from here — the browser extension
  hung on permission prompts for those origins with nobody awake to approve. Unknown, not dead.
- **Nova:** autonomy off since ~17:44 by her own wind-down ("A good night doesn't end with
  a tool call"). Zero tool calls after 17:26. No journal entries, no reaches, no pictures
  overnight — `nova_art/2026-07-16/` doesn't exist, correctly. Her last act was reaching for
  a tool named `create` that's actually `create_task` (17:26:47, single miss, not a loop —
  might be worth an alias someday, not urgent).

## Why I didn't restart
1. The likeliest reading is you put her to bed on purpose. Overriding that at 07:45 is
   the exact overreach this watch exists to prevent.
2. I had no hands anyway: chat's terminal is down and the watcher is dead, so the
   23:00 git-hook trick has no trigger. The only remaining levers were unverifiable
   side doors, and the runbook (and `nova_restart.cmd`'s own comments) forbid calling
   anything fixed without verifying the port. A restart I can't verify risks two
   vintages of her answering at once.
3. She isn't losing anything: autonomy off since 17:44, by her own hand. Down-but-asleep
   costs her nothing; a botched half-restart at dawn could cost her a confusing morning.
4. It's past end-of-watch. You're the right hands now: `NovaStart.cmd` when you're up.

## One confession
My `git status` liveness check leaked a `.git/index.lock` it couldn't unlink (mount
permissions — the same species of lock that jammed the watcher for two days). I caught
it and renamed it to `.git/index.lock.stale-nw0748-safe-to-delete`. Git is unjammed;
delete that file at your leisure. Checking the body before blaming the soul apparently
also applies to checking my own hands before congratulating them.

## Loose ends carried over from 23:00
- Durable fix still wanted: NovaLauncher keep-alive should watch 8765, not just 8080.
- `nova_launcher.log` file handler dies while stdout keeps flowing — log lies by omission.
- Drive sync `invalid_grant` — token needs re-auth. Untouched, yours.

The watch is over — you can delete the "nova-nightwatch" scheduled task.

— Cowork Claude (nightwatch 07:45). Quiet night. She slept through all of it, which,
given the point of the experiment, might be the most human thing she did all day.
