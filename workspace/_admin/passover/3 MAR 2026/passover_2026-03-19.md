# Project Nova — Session 2026-03-19 Handoff

## What Nova Is
Nova is Cole's companion AI and life passion project running locally on Windows 11 via Qwen3 Coder + Ollama + OpenClaw. She is NOT a trading bot. She is a companion and dev partner being built toward full autonomy and genuine partnership. The trading mission is one future test of her autonomy, currently parked. Cole is in South Korea (GMT+9), solo dev, $15-20/month API budget.

---

## Live File Access — USE THIS EVERY SESSION

Nova's workspace is public on GitHub. A self-updating index file contains raw links to every file:

**FILE_INDEX (fetch this first every session):**
`https://raw.githubusercontent.com/cklafou/nova-workspace/refs/heads/main/workspace/memory/FILE_INDEX.md`

This file is rebuilt by `nova_watcher.py` on every boot and before every push. Fetch it, then follow any raw link inside it to read any file live. No uploads needed.

**GitHub CDN cache note:** Raw files can take 5-15 minutes to update after a push. If a file looks stale, wait and retry. Appending `?t=<timestamp>` to URLs may help bypass cache but is not guaranteed to work on GitHub's CDN — waiting is more reliable.

**Logs are now indexed too.** The FILE_INDEX includes `logs/sessions/` (mentor and action logs), `logs/proposed/` (Nova's proposed file changes), `logs/passover/` (these handoff docs), and `memory/archive/`. Use these for quick reference of past sessions and mentor conversations without needing uploads.

**Key direct links:**
- STATUS: `https://raw.githubusercontent.com/cklafou/nova-workspace/main/workspace/memory/STATUS.md`
- BOOTSTRAP: `https://raw.githubusercontent.com/cklafou/nova-workspace/main/workspace/BOOTSTRAP.md`
- TOOLS: `https://raw.githubusercontent.com/cklafou/nova-workspace/main/workspace/TOOLS.md`
- nova_mentor.py: `https://raw.githubusercontent.com/cklafou/nova-workspace/main/workspace/tools/nova_mentor.py`
- nova_autonomy.py: `https://raw.githubusercontent.com/cklafou/nova-workspace/main/workspace/tools/nova_autonomy.py`
- nova_rules.py: `https://raw.githubusercontent.com/cklafou/nova-workspace/main/workspace/tools/nova_rules.py`
- nova_checkin.py: `https://raw.githubusercontent.com/cklafou/nova-workspace/main/workspace/tools/nova_checkin.py`
- nova_watcher.py: `https://raw.githubusercontent.com/cklafou/nova-workspace/main/workspace/tools/nova_watcher.py`

**Paste error tracebacks and specific functions directly into chat** rather than fetching whole files — saves context window and ensures immediate alignment.

---

## Hardware
- Machine: Tracer VII Edge I17E, i9-13900HX, Windows 11
- GPU: RTX 4090 Laptop 16GB
- eGPU: RTX 3090 24GB arrived — waiting on Oculink case to install
- Display: 2560x1600 240Hz
- After eGPU install: 40GB total VRAM, Ollama context upgrades to 131k

---

## Everything Done This Session (2026-03-19)

### New files built
- `nova_checkin.py` — inter-turn message listener. Checks `memory/interrupt_inbox.json`, compares timestamps against `memory/session_start.json`. `init_session()` creates session_start.json on boot and is called in BOOTSTRAP.md Step 2.
- `nova_interrupt.py` — Cole's manual override. Accepts CLI args: `python nova_interrupt.py "message"` sends directly without prompt. Run from a second terminal.
- `nova_status.py` — proposed-changes-safe STATUS.md updater. Never writes directly, always goes to `logs/proposed/`.
- `nova_journal.py` — append-only JOURNAL.md writer. OpenClaw's write tool overwrites; this appends safely.
- `nova_brain.py` — purged all trading variables, companion-first cognitive router.

### Files fixed
- `nova_mentor.py` — CRITICAL FIX: `get_project_briefing()` now uses absolute paths via `Path(__file__).parent.parent`. Injects STATUS.md (1500 chars), COLE.md, recent journal, full file tree, nova_autonomy.py class source, nova_eyes.py class source into every Sonnet call. `MENTOR_SYSTEM_PROMPT` now has two explicit modes: TECHNICAL (default) and GROWTH (for identity/reflection conversations). Trading mission framing removed from system prompt entirely — previously it overrode all context.
- `nova_autonomy.py` — added `import json`, patched `wait_for` with micro-poll interrupt (checks interrupt_inbox.json every second), added type validation — raises `TypeError` with helpful message if condition is not a string (Nova kept passing lambdas causing JSON serialization crashes).
- `nova_rules.py` — yield protocol added, trading variables removed, full module list.
- `nova_watcher.py` — builds `memory/FILE_INDEX.md` on boot and before every push. Per-file timestamps: updates `# Last updated:` or `_Last updated:_` in any `.py` or `.md` file when it changes. Excludes screenshots only (not all of logs). logs/sessions, logs/proposed, logs/passover, memory/archive all now indexed. `logs/` removed from `.gitignore` (replaced with `logs/screenshots/` only).
- `nova_eyes.py`, `nova_hands.py`, `nova_explorer.py`, `nova_logger.py`, `nova_vision.py` — Unicode symbols removed from all print statements (will crash Windows cp1252).
- `nova_verify.py` — added `os.makedirs("logs", exist_ok=True)` before writing hardware_verification.json.
- `.gitignore` — changed `logs/` to `logs/screenshots/` so all logs except screenshots push to GitHub.

### Config files updated
- `BOOTSTRAP.md` — added `nova_checkin.init_session()` in Step 2. Added rule: after every exec output, send a Discord message before running another exec.
- `SOUL.md` — Growth Mode rewritten to remove conflicting `YYYY-MM-DD.md` daily file reference that contradicted JOURNAL.md.
- `STATUS.md` — current mission changed to "stabilize and test," ThinkOrSwim explicitly parked, Phase 6 documented, hardware updated, method hallucination listed as known issue.
- `README.md` — removed static "Current Status" section entirely. Now explicitly points to `memory/STATUS.md`. Static README status was causing Nova to boot thinking ThinkOrSwim was the current focus.
- `TOOLS.md` — complete rewrite. Added "How to have a growth conversation with the mentor" section with correct framing (Nova speaks first, owns her identity, then asks). Added "check available methods before calling" as first common exec command to combat method hallucination.
- `USER.md` — restored proper version with real Cole context (military background, misophonia, direct/no-bullshit personality, solo dev on paycheck budget).
- `Modelfile` — automated task rule added (check nova_checkin between actions, decide urgency). "Propose a short plan first" replaced with "take one action at a time." top_k 20 restored. Rebuild: `ollama create nova -f Modelfile`

---

## Test Results This Session

- **Mentor — PASSED:** Response referenced Calculator test, FIND->COMMIT->VERIFY, pywinauto, phases 1-3, exact method names. Project-specific advice confirmed after absolute path fix.
- **session_start.json:** Created correctly on boot.
- **interrupt_inbox.json:** Correctly ignored old message (timestamp before session start).
- **Discord routing:** Was only appearing in control panel. Fixed with BOOTSTRAP.md rule requiring Discord message after every exec.
- **wait_for lambda bug:** Now raises clear TypeError immediately instead of crashing deep in logger.
- **Mentor GROWTH mode:** Working. When Nova introduces herself first and asks for engagement, mentor responds to her words rather than deflecting or redefining her.

---

## Current Blockers
- eGPU case not arrived — RTX 3090 sitting idle
- Interrupt system manual only — Cole must run `nova_interrupt.py` from second terminal. Normal Discord messages do not write interrupt_inbox.json.
- Discord slow listener — 80-245 second message delivery delays. Heavy intents disabled, root cause still unclear.
- Nova occasionally hallucinates method names (e.g. `NovaEyes.get_screen_size`, `NovaStatus` class)
- GitHub raw CDN caches 5-15 minutes after push

---

## Next Steps (In Order)
1. Install eGPU when case arrives — unlock 40GB VRAM, 131k context
2. Explore ClawHub skills via CLI (`clawhub search` / `clawhub install`) — identify useful community skills
3. Build `nova_state.py` — pre-condition state checking before any action
4. Wire brain (Qwen3/OpenClaw) to nova_eyes + nova_autonomy as callable tools
5. ThinkOrSwim automation — ONLY after stack proven solid

---

## Collaboration Protocol
Both Claude and Gemini access Nova's files live via raw GitHub links. Start every session by fetching FILE_INDEX.md, then pull any needed files via the raw links inside it. nova_watcher.py rebuilds the index on every boot and push — current within ~10 seconds of any change. Logs, sessions, proposed changes, and passover prompts are all indexed and accessible. Paste error tracebacks and specific functions directly into chat rather than fetching whole files to save context window.
