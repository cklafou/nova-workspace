# Project Nova — Session 2026-03-20 Handoff

## What Nova Is
Nova is Cole's companion AI and life passion project running locally on Windows 11 via Qwen3 Coder + Ollama + OpenClaw. She is NOT a trading bot. She is a companion and dev partner being built toward full autonomy and genuine partnership. The trading mission is one future test of her autonomy, currently parked. Cole is in South Korea (GMT+9), solo dev, $15-20/month API budget.

---

## For Claude — Live File Access (GitHub Commit-Hash URLs)

Paste this URL at the start of every Claude session:
```
https://raw.githubusercontent.com/cklafou/nova-workspace/main/workspace/memory/FILE_INDEX_LINK.md
```

Claude fetches it, reads the commit-hash FILE_INDEX URL from the content, fetches that index, and has full live access to every workspace file. All URLs in FILE_INDEX use commit hashes — completely cache-proof, no CDN delays ever. Every file URL in the index is directly fetchable.

**How it works:**
1. Fetch FILE_INDEX_LINK.md (permanent URL, never changes)
2. The content is a single line: a commit-hash raw URL to FILE_INDEX.md
3. Fetch that URL — get the full workspace index with live links to every file
4. Fetch any file directly from the index links

nova_watcher.py prints the Claude SESSION URL on every boot as a convenience — but FILE_INDEX_LINK.md is the permanent bootstrap that works across all sessions.

---

## For Gemini — Live File Access (Google Drive Native Mirror)

Nova's entire workspace is live-synced to a Google Drive folder named `Nova_Workspace`. Do NOT attempt to fetch URLs. Instead, use your internal Google Workspace integration to read files directly.

**Boot Sequence:**
1. `@Google Workspace: Search my Google Drive for the file named STATUS.md` to get the current mission state.
2. `@Google Workspace: Search my Google Drive for the file named JOURNAL.md` to see recent session history.
3. `@Google Workspace: Search my Google Drive for the file named FILE_INDEX.md` to understand the current workspace structure.

**File Navigation Rule:**
The Drive mirror uses a real folder hierarchy under `Nova_Workspace/workspace/` (no flattened filenames). However, since your integration is search-based, do not try to navigate folders. When asked to read a file, simply search for its exact filename (e.g., search Drive for `nova_autonomy.py`). This will pull the raw text of that file instantly regardless of how deep it is nested.

**Drive folder (share link):**
```
https://drive.google.com/drive/folders/1GLW6qVm5PHp_xnSlEXlnZIBhhmixzFya?usp=sharing
```

nova_drive.py syncs the workspace to Drive on every nova_watcher push using diff-based sync (MD5 checksums — only changed files uploaded). The mirror is always current within ~10 seconds of any file change.

---

## Hardware
- Machine: Tracer VII Edge I17E, i9-13900HX, Windows 11
- GPU: RTX 4090 Laptop 16GB
- eGPU: RTX 3090 24GB arrived — waiting on Oculink case to install
- After eGPU install: 40GB total VRAM, Ollama context upgrades to 131k

---

## Everything Done This Session (2026-03-20)

### New files built
- `nova_log_reader.py` — reads session logs. `summarize_today()`, `get_failures(days)`, `get_recent_sessions(days)`. Nova runs before mentor conversations for real failure data.
- `nova_state.py` — pre-condition state checking before any action.
- `nova_stress_tester.py` — Nova built autonomously overnight. Functional string handling tests. Kept in tools/.
- `nova_drive.py` — Google Drive workspace mirror. Diff-based sync (MD5 checksums). Full folder hierarchy under `Nova_Workspace/workspace/`. OAuth2 as Cole's account. Credentials: `C:\Users\lafou\OneDrive\Documents\client_secrets.json`, token auto-saved after first browser login.
- `nova_backup.py` — automated backups. Session snapshots (critical memory files) on every boot pushed to Drive `backups/sessions/`. Weekly full backup every Sunday pushed to Drive `backups/weekly/`. 30 session / 4 weekly retained locally.

### Files updated
- `nova_mentor.py` — GROWTH MODE in system prompt. `history` param in `ask()`. `start_conversation()` helper. ThinkOrSwim framing removed.
- `nova_journal.py` — `sanitize()` strips apostrophes. Same-day header detection.
- `nova_watcher.py` — commit-hash URLs in FILE_INDEX. FILE_INDEX_LINK.md written every push. Two-step push cycle. Full workspace skeleton. Calls nova_drive.py and nova_backup.py on boot/push. Prints both advisor URLs on boot.
- `STATUS.md` — Phase 6, all new tools, Modelfile rebuild at top of next steps.
- `JOURNAL.md` — cleaned from 5 contradictory same-day entries to 1 honest entry.

---

## Test Results This Session
- Mentor GROWTH MODE — working, calls out Q&A-without-responding pattern directly.
- Heartbeat — clean HEARTBEAT_OK without running health checks.
- Journal same-day header detection — working correctly.
- Journal sanitize() — apostrophes stripped automatically.
- GitHub commit-hash URLs — fully working, zero CDN lag.
- FILE_INDEX_LINK.md two-fetch bootstrap — confirmed working for Claude.
- Google Drive diff-based sync — confirmed working (shows UPLOAD/DELETE per file).

---

## Current Blockers
- eGPU case not yet arrived — RTX 3090 sitting idle
- **Modelfile updated but model NOT rebuilt** — run: `ollama create nova -f Modelfile`
- Interrupt system is manual only
- Nova occasionally hallucinates method names
- Discord slow listener — 80-245 second delays

---

## Permanent URLs

| Purpose | URL |
|---|---|
| Claude bootstrap | `https://raw.githubusercontent.com/cklafou/nova-workspace/main/workspace/memory/FILE_INDEX_LINK.md` |
| Gemini Drive folder | `https://drive.google.com/drive/folders/1GLW6qVm5PHp_xnSlEXlnZIBhhmixzFya?usp=sharing` |

---

## Next Steps (In Order)
1. **Run: `ollama create nova -f Modelfile`** — rebuild model with updated Modelfile
2. Install eGPU when case arrives — unlock 40GB VRAM and 131k context
3. Explore ClawHub skills — identify useful community skills
4. Wire brain (Qwen3/OpenClaw) to nova_eyes + nova_autonomy as callable tools
5. ThinkOrSwim automation — ONLY after stack proven solid

---

## Collaboration Protocol
Both advisors have live workspace access through their respective optimal paths. nova_watcher.py must be running for both GitHub pushes and Drive sync to stay current. Paste error tracebacks directly into chat rather than fetching whole files to save context window.
