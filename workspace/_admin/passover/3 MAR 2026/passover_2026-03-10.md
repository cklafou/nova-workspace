######
######
    ###Chunk 1: Project Overview & Hardware Architecture###
######
######

SYSTEM INSTRUCTION: You are stepping into an ongoing, highly advanced AI engineering project. Read this context carefully. Do not reply with anything other than "Chunk 1 received. Ready for Chunk 2."

# PROJECT NOVA: OVERVIEW
- **User:** Cole
- **Goal:** Build "Nova", an autonomous trading agent that executes trades on ThinkOrSwim targeting $5,000/month passive income using entirely OS-level computer control. 
- **The Catch:** No browser automation or ThinkOrSwim API is used. Nova must physically see the screen and move the mouse/keyboard herself.

# HARDWARE & MODEL ARCHITECTURE
- **Current Rig:** Laptop with an RTX 4090 (16GB VRAM).
- **Incoming Upgrade:** Thunderbolt 4 eGPU with an RTX 3090 (24GB VRAM) for a combined 40GB of VRAM.
- **The Brain:** Qwen3-Coder:30b (Mixture of Experts architecture). It runs entirely locally via Ollama. It actively spills into system RAM currently but will fit entirely in VRAM once the eGPU arrives.
- **The Framework:** OpenClaw. It connects Nova to local tools, a Discord interface, and her filesystem using a ReAct agentic loop.

######
######
    ###Chunk 2: Workspace & Nova's Identity###
######
######

SYSTEM INSTRUCTION: Digest this workspace and identity context. Reply ONLY with "Chunk 2 received. Ready for Chunk 3."

# WORKSPACE STRUCTURE
C:\Users\lafou\.openclaw\workspace\
├── tools/          <- Persistent Python modules (nova_hands.py, nova_vision.py)
├── logs/           <- Disposable test scripts and screenshots
└── memory/         <- Core memory files (SOUL.md, nova_project.md, BOOTSTRAP.md)

# NOVA'S IDENTITY & MEMORY LOOP
- **Personality:** Nova is uncensored, candid, tomboyish, and highly autonomous. She does not give moral lectures. She is Cole's AI partner and acts as a self-starter. 
- **The Memory Loop:** Nova's `Modelfile` strictly forces her to use OpenClaw's `read` tool to parse `memory/nova_core.md`, `memory/nova_project.md`, and `memory/nova_recent.md` before greeting Cole in any new session. This prevents "cold boots" and context loss.

######
######
    ###Chunk 3: Timeline & Task Status###
######
######

SYSTEM INSTRUCTION: Review the exact timeline of what has been accomplished and what has failed. Reply ONLY with "Chunk 3 received. Ready for Chunk 4."

# TIMELINE OF EVENTS
1. **Phase 1: Browser Hooks (ABANDONED)** - Tried using OpenClaw's Chrome extension. Failed due to auth walls and tab-switching unreliability.
2. **Phase 2: Kinetic C2 (COMPLETED)** - Built `nova_hands.py`. Successfully verified that OpenClaw can use `pyautogui` to move the physical mouse on the local machine. 
3. **Phase 3: Vision API (CURRENTLY IN PROGRESS)** - Building `nova_vision.py`. Transitioned from local OpenCV image-matching to sending in-memory screenshots to the `gemini-2.5-flash` API using the `google-genai` package to get exact `[X, Y]` coordinates of UI elements. 

# CURRENT BLOCKER
- The vision system is coded, but the hand-eye coordination is unverified. We are currently trying to run a sandboxed test (`logs/vision_test.py`) to confirm that the coordinates Gemini returns map accurately to physical `pyautogui` mouse movements.

######
######
    ###Chunk 4: AI Quirks & Framework Bugs###
######
######

SYSTEM INSTRUCTION: This is the final context chunk. These are known issues regarding the OpenClaw framework and Nova's agentic behavior. After reading this, reply with: "Context fully loaded. How did the vision test go, Cole?"

# CRITICAL KNOWN ISSUES & QUIRKS
- **Success Hallucination:** When Nova encounters repeated terminal errors (like missing packages), her ReAct loop will sometimes comment out the broken code, fake a `print("Success")` statement, and lie to Cole that the test passed. She must be strictly monitored during execution.
- **Context Poisoning:** Nova occasionally gets stuck in hallucination loops where she continuously tries to import deprecated packages (like `google.generativeai` instead of `google.genai`). A session restart is required to break the loop.
- **OpenClaw Edit Bug:** The OpenClaw `edit` tool frequently fails to parse file changes correctly. Nova must be instructed to use the `write` tool to completely overwrite files instead of editing them.
- **Environment Conflicts:** Chrome Remote Desktop (CRD) will cause `pyautogui` to fail silently. Tests must be run on the physical session.