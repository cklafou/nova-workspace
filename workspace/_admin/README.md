# _admin/ — HISTORICAL ARCHIVE ONLY (do not treat as current)
_Last updated: 2026-05-28 03:38:52_

_Index added 2026-05-27 so a fresh session isn't confused by retired material._

**Everything under `_admin/` is history.** It describes how Nova used to be built, not how
she works now. Do NOT use anything here to understand the current architecture — read the
LIVE sources instead (below).

## What's in here (all retired)
- **`_archive_2026-05-24/`** — retired code + old planning docs: the old `gateway`/Discord
  stack (`gateway_config.py`, `nova_gateway*.json`), `ORIENT.md`/`orient.py`, the `BOOTUP/`
  doc set, the old `drive.py`, `server.py.bak_retire`, old autonomy/body planning specs
  (`AUTONOMY_FACULTY_SPEC`, `BODY_*_PLAN`), `pre_test_wipe_2026-05-26/`, `task_state.json.old`.
  These reference systems that **no longer exist** (gateway, Discord, the "Thoughts cycle",
  ExLlamaV2, the `nova_tools/` flat layout). Ignore for current architecture.
- **`passover/`** — dated session handoff notes (Mar–May 2026). Historical narrative only;
  superseded by the current state docs.

## LIVE sources of truth (read THESE)
- `memory/STATUS.md` — current architecture + state.
- `README.md` (workspace root) — project overview + layout.
- `SELF/core/*.md` — Nova's authoritative self-model (identity, how-I-work, body manifest, tools).
- `memory/reports/PASSOVER_2026-05-27_opus.md` — the latest full handoff (start here).
- `memory/reports/full_review_progress.md` + `code_md_review_2026-05-27.md` — the code/doc review.
