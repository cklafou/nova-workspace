# _t_bad

## GAP
Deliberately broken tool used to prove the forge contains its own failures instead of propagating them into the router.

## SHAPE
run() -> raises.

## TEST
Calling it must return an ERROR string containing the traceback, never crash.
