# zztest

## GAP
I kept needing to add two numbers mid-task and had no arithmetic limb, so I shelled out to PowerShell for something trivial and burned a tool loop each time.

## SHAPE
run(a:int,b:int) -> str. Returns the sum as a string. Errors return 'ERROR: ...'.

## TEST
2+3 must return '5'. Non-numeric input returns an ERROR string, never raises.
